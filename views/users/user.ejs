<%- include("../partials/header") %>

<div class="row">
  <!-- Left Colomn -->
  <%- include("./userPartials/leftColumn") %>
  <!-- Left Column End -->
  
  <!-- Right Column -->
  <div class="col-md-8 col-sm-12">
    <!-- Favorite School -->
    <div class="card text-center mb-2">
      <div class="card-body">
        <h3 class="card-title mx-auto">
          <% if (mypage) { %>
          Your Favorite School
          <% } else { %>
          <%= user.name %>'s Favorite School
          <% } %>
        </h3>
        <hr>
        <div class="row row-cols-1 row-cols-md-3">
          <% schools.forEach(school => { %>
          <div class="col mb-4">
            <a href="/schools/<%= school.slug %>" class="card-link">
              <div class="card">
                <img src="/<%= school.image %>" class="card-img-top" alt="No Image">
                <div class="card-body">
                  <h5 class="card-title">
                    <%= school.name %>
                  </h5>
                  <% if (!school.averageRating) { %>
                  <p>
                    No Rating Yet
                  </p>
                  <% } else { %>
                  <p>
                    <span>
                      <i class="fas fa-star <% if (school.averageRating>= 1.0) { %> checked <% } %>"></i>
                      <i class="fas fa-star <% if (school.averageRating>= 1.5) { %> checked <% } %>"></i>
                      <i class="fas fa-star <% if (school.averageRating>= 2.5) { %> checked <% } %>"></i>
                      <i class="fas fa-star <% if (school.averageRating>= 3.5) { %> checked <% } %>"></i>
                      <i class="fas fa-star <% if (school.averageRating>= 4.5) { %> checked <% } %>"></i>
                    </span>
                  </p>
                  <% } %>
                  <span class="badge badge-secondary mb-2">
                    <i class="fas fa-thumbs-up"></i> Like: <%= school.likes.length %>
                  </span>
                  <span class="badge badge-secondary mb-2">
                    <i class="fas fa-comment-dots"></i> Review: <%= school.reviews.length %>
                  </span>
                </div>
              </div>
            </a>
          </div>
          <% }) %>
        </div>
      </div>
    </div>
    <!-- Favorite School End -->

    <!-- Favorite School -->
    <div class="card text-center mb-2">
      <div class="card-body">
        <h3 class="card-title mx-auto">
          <% if (mypage) { %>
          Your Favorite School
          <% } else { %>
          <%= user.name %>'s Favorite School
          <% } %>
        </h3>
        <hr>
        <div class="row row-cols-1 row-cols-md-3">
          <% schools.forEach(school => { %>
          <div class="col mb-4">
            <a href="/schools/<%= school.slug %>" class="card-link">
              <div class="card">
                <img src="/<%= school.image %>" class="card-img-top" alt="No Image">
                <div class="card-body">
                  <h5 class="card-title">
                    <%= school.name %>
                  </h5>
                  <% if (!school.averageRating) { %>
                  <p>
                    No Rating Yet
                  </p>
                  <% } else { %>
                  <p>
                    <span>
                      <i class="fas fa-star <% if (school.averageRating>= 1.0) { %> checked <% } %>"></i>
                      <i class="fas fa-star <% if (school.averageRating>= 1.5) { %> checked <% } %>"></i>
                      <i class="fas fa-star <% if (school.averageRating>= 2.5) { %> checked <% } %>"></i>
                      <i class="fas fa-star <% if (school.averageRating>= 3.5) { %> checked <% } %>"></i>
                      <i class="fas fa-star <% if (school.averageRating>= 4.5) { %> checked <% } %>"></i>
                    </span>
                  </p>
                  <% } %>
                  <span class="badge badge-secondary mb-2">
                    <i class="fas fa-thumbs-up"></i> Like: <%= school.likes.length %>
                  </span>
                  <span class="badge badge-secondary mb-2">
                    <i class="fas fa-comment-dots"></i> Review: <%= school.reviews.length %>
                  </span>
                </div>
              </div>
            </a>
          </div>
          <% }) %>
        </div>
      </div>
    </div>
    <!-- Favorite School End -->

    <!-- Favorite School -->
    <div class="card text-center mb-2">
      <div class="card-body">
        <h3 class="card-title mx-auto">
          <% if (mypage) { %>
          Your Favorite School
          <% } else { %>
          <%= user.name %>'s Favorite School
          <% } %>
        </h3>
        <hr>
        <div class="row row-cols-1 row-cols-md-3">
          <% schools.forEach(school => { %>
          <div class="col mb-4">
            <a href="/schools/<%= school.slug %>" class="card-link">
              <div class="card">
                <img src="/<%= school.image %>" class="card-img-top" alt="No Image">
                <div class="card-body">
                  <h5 class="card-title">
                    <%= school.name %>
                  </h5>
                  <% if (!school.averageRating) { %>
                  <p>
                    No Rating Yet
                  </p>
                  <% } else { %>
                  <p>
                    <span>
                      <i class="fas fa-star <% if (school.averageRating>= 1.0) { %> checked <% } %>"></i>
                      <i class="fas fa-star <% if (school.averageRating>= 1.5) { %> checked <% } %>"></i>
                      <i class="fas fa-star <% if (school.averageRating>= 2.5) { %> checked <% } %>"></i>
                      <i class="fas fa-star <% if (school.averageRating>= 3.5) { %> checked <% } %>"></i>
                      <i class="fas fa-star <% if (school.averageRating>= 4.5) { %> checked <% } %>"></i>
                    </span>
                  </p>
                  <% } %>
                  <span class="badge badge-secondary mb-2">
                    <i class="fas fa-thumbs-up"></i> Like: <%= school.likes.length %>
                  </span>
                  <span class="badge badge-secondary mb-2">
                    <i class="fas fa-comment-dots"></i> Review: <%= school.reviews.length %>
                  </span>
                </div>
              </div>
            </a>
          </div>
          <% }) %>
        </div>
      </div>
    </div>
    <!-- Favorite School End -->

  </div>
  <!-- Right Column End -->
</div>

<%- include("../partials/footer") %>